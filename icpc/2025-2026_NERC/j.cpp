#include <bits/stdc++.h>
using namespace std;

using ll=long long;
using db=double;
const ll N=1e5+5;

void solve(){
    int n,k;
    cin >> n >> k;
    db sum=0;
    
    cout << setprecision(12) << max(0.0, sum*1000.0 - 1000.0);
}

int main (){
    ios::sync_with_stdio(0); cin.tie(0);
    int t=1;
    // cin >> t;
    while (t--){
        solve();
    }
}
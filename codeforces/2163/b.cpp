#include <bits/stdc++.h>
using namespace std;

using ll=long long;
using pll=pair<ll,ll>;
const ll N=2e5+5;
int a[N];

void solve(){
    ll n;
    string s;
    cin >> n;
    for (int i=1;i<=n;i++) cin >> a[i];
    cin >> s;
    
}

int main (){
    ios::sync_with_stdio(0); cin.tie(0);
    ll t;
    cin >> t;
    while (t--){
        solve();
    }
}
#include <bits/stdc++.h>
using namespace std;
using ll=long long;
const ll K=35,inf=1e15;
ll n,k,p;
struct mtx{
    vector<vector<ll>> a=vector<vector<ll>>(K,vector<ll> (K,inf));
    mtx operator*= (const mtx &o){
        mtx re;
        for (int i=0;i<k;i++){
            for (int j=0;j<k;j++){
                for (int m=0;m<k;m++){
                    
                }
            }
        }
        return re;
    }
};

int main (){
    ios::sync_with_stdio(0); cin.tie(0);
    cin >> n >> k >> p;
}

/*
0 1 2 3 4

*/